from features import feature_user_photos

def photo_expo_clustering(user_photo_features):
    """Clustering exposure of user's photos

    :param: user_photo_features
        clustering features extracted from user photos
            {photo1: (f_expo, f_obj), ...}

    :return:

    """
    pass