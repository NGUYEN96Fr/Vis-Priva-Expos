
[main.py]

# path to train & test datasets
train_test_path = process_raw_data/out/train_test_split.json

# path to processed ground-truth user exposure profiles
gt_expo_path =  process_raw_data/out/gt_usr_exposure.json

# object exposure scores in each situation
situation_path = process_raw_data/raw_data/crowdsourcing_object_per_situation_scores/processed_situations

#outdir
outdir = out/
